document.addEventListener('DOMContentLoaded',()=>{const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();const menu=document.querySelector('.menu-button');const nav=document.getElementById('site-nav');if(menu&&nav){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));menu.textContent=open?'Close':'Menu';});}const form=document.getElementById('walkthrough-form');if(form){form.addEventListener('submit',(event)=>{event.preventDefault();if(!form.checkValidity()){form.reportValidity();return;}const data=new FormData(form);const lines=[`Name: ${data.get('name')}`,`Phone: ${data.get('phone')}`,`Email: ${data.get('email')}`,`Property type: ${data.get('property_type')}`,`Property address: ${data.get('address')}`,`Primary concern: ${data.get('concern')}`,`Details: ${data.get('details')||'Not provided'}`,`Preferred appointment window: ${data.get('availability')||'Not provided'}`];const subject=encodeURIComponent('Property Hardening Walkthrough Request');const body=encodeURIComponent(lines.join('\n'));window.location.href=`mailto:contact@restomatics.com?subject=${subject}&body=${body}`;const status=document.getElementById('form-status');status.textContent='Your email app should now be open with your request ready to send. If it did not open, call or text (305) 240-0230.';});}});